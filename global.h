#pragma once

#include <stdbool.h>

int increment_floor(char *);
int decrement_floor(char *);
int floor_to_int(const char *);
int floor_in_range(const char *, const char *, const char *);
bool is_valid_floor(const char *);
